# rename_pdfs_easily

## How to use?
Clone the repo:
```
git clone https://github.com/yousif-saif/rename_pdfs_easily
```
First of all add all of your pdfs to the `./pdfs` folder

Then run the local flask server by running this command:
```
python app.py
```

Thats it!
